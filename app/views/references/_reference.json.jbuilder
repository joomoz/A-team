json.extract! reference, :id, :key, :author, :title, :journal, :year, :volume, :number, :pages, :month, :note, :publisher, :series, :address, :edition, :booktitle, :editor, :organization, :created_at, :updated_at
json.url reference_url(reference, format: :json)
