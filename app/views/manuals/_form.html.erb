<%= form_for(@manual) do |f| %>
  <% if @manual.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manual.errors.count, "error") %> prohibited this manual from being saved:</h2>

      <ul>
      <% @manual.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.hidden_field :entry_type, :value => "manual" %>

    <h3>Required fields</h3>

    <div class="field">
      <%= f.label :key %><br>
      <%= f.text_field :key %>
    </div>

    <div class="field">
      <%= f.label :title %><br>
      <%= f.text_area :title %>
    </div>
    <h3>Optional fields</h3>
    <div class="field">
      <%= f.label :author %><br>
      <%= f.text_area :author %>
    </div>
    <div class="field">
      <%= f.label :year %><br>
      <%= f.number_field :year %>
    </div>
    <div class="field">
      <%= f.label :edition %><br>
      <%= f.text_area :edition %>
    </div>
    <div class="field">
      <%= f.label :address %><br>
      <%= f.text_area :address %>
    </div>
    <div class="field">
      <%= f.label :month %><br>
      <%= f.number_field :month %>
    </div>
    <div class="field">
      <%= f.label :organization %><br>
      <%= f.text_area :organization %>
    </div>

    <div class="field">
      <%= f.label :note %><br>
      <%= f.text_area :note %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>