<%= form_for(@bibtex) do |f| %>
  <% if @bibtex.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bibtex.errors.count, "error") %> prohibited this bibtex from being saved:</h2>

      <ul>
      <% @bibtex.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
