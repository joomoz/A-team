<%= form_for(@misc) do |f| %>
  <% if @misc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@misc.errors.count, "error") %> prohibited this misc from being saved:</h2>

      <ul>
      <% @misc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.hidden_field :entry_type, :value => "misc" %>

    <h3>Required fields</h3>

    <h3>Optional fields</h3>

    <div class="field">
      <%= f.label :author %><br>
      <%= f.text_area :author %>
    </div>
    <div class="field">
      <%= f.label :title %><br>
      <%= f.text_area :title %>
    </div>
    <div class="field">
      <%= f.label :howpublished %><br>
      <%= f.text_area :howpublished %>
    </div>
    <div class="field">
      <%= f.label :month %><br>
      <%= f.number_field :month %>
    </div>
    <div class="field">
      <%= f.label :year %><br>
      <%= f.number_field :year %>
    </div>
    <div class="field">
      <%= f.label :note %><br>
      <%= f.text_area :note %>
    </div>
    <div class="field">
      <%= f.label :key %><br>
      <%= f.text_field :key %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
